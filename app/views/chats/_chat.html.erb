<div class="d-flex justify-content-between align-items-center mb-2", id="<%= dom_id(chat) %>">
  <%= link_to chat.name, chat_path(chat), class: "text-white flex-grow-1 text-decoration-none" %>
  <%= button_to "Delete", chat_path(chat), method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "btn btn-sm btn-danger ms-2" %>
</div>
